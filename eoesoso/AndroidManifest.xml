<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eoesou"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="7" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen" >
        <activity
            android:name=".ui.PageHomeActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.SidebarFragmentActivity" />
        <activity android:name=".ui.NotificationActivity" />
        <activity android:name=".ui.MyLikeActivity" />
        <activity android:name=".ui.MyPageHomeActivity" />
        <activity android:name=".ui.SetActivity" />
        <activity android:name=".ui.CameraActivity" />
        <activity android:name=".ui.PageHomeFriend" />
        <activity android:name=".ui.ShareActivity" />
        <activity android:name=".ui.PhotoDetailsActivity" />
        <service android:name=".service.SosoService"></service>
    </application>

</manifest>