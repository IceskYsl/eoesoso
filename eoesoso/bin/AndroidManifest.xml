<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eoesou"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="7" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen" >
        <activity
            android:name=".PageHomeActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SidebarFragmentActivity" />
        <activity android:name=".NotificationActivity" />
        <activity android:name=".MyLikeActivity" />
        <activity android:name=".MyPageHomeActivity" />
        <activity android:name=".SetActivity" />
        <activity android:name=".CameraActivity" />
        <activity android:name=".PageHomeFriend" />
        <activity android:name=".ShareActivity" />
        <activity android:name=".ui.PhotoDetailsActivity" />
    </application>

</manifest>